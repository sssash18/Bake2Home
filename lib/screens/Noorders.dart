import 'package:flutter/material.dart';

class NoOrders extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Not Accepting Orders"),
    );
  }
}